- defaults:
    link:
      relink: true
#      relative: true

- clean: ['~']

- link:
    ~/.emacs: emacs
    ~/.emacs.d: emacs.d

- shell:
  - [ git submodule update --init --recursive, Installing submodules]
